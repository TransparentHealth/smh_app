{
  "name": "smh-style",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.8.1",
    "@fortawesome/fontawesome-svg-core": "^1.2.17",
    "axios": "^0.18.1",
    "bootstrap": "^4.3.1",
    "browserify": "^16.2.3",
    "node-sass": "^4.11.0"
  },
  "scripts": {
    "build:js": "browserify js/main.js > dist/App.js",
    "build:sass": "node-sass -o dist sass/",
    "build": "npm run build:js && npm run build:sass",
    "build:watch": "watch 'npm run build' ."
  },
  "eslintConfig": {
    "extends": "react-app",
    "allowImportExportEverywhere": true
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {}
}
